{
  "schemaVersion": 5,
  "user": {
    "codingLevel": "new"
  },
  "project": {
    "uiFramework": "shadcn",
    "_uiFrameworkNote": "React 18/19 + Tailwind v4. globals.css is a shadcn-shaped token export, so component variant vocabulary follows shadcn naming."
  },
  "workspace": {
    "name": "Jake UI",
    "localPath": ".",
    "stage": "github",
    "origin": "existing-repo",
    "detectedLayers": {
      "monorepo": false,
      "storybook": false,
      "tokens": true,
      "syncLayer": false
    }
  },
  "figma": {
    "mechanism": "console-mcp",
    "fileKey": "ovnLtL9xbX8SG5xDw673Un",
    "connected": true,
    "lastVerified": "2026-08-09T05:49:04Z",
    "coverPageBuilt": false,
    "canPublish": null,
    "libraryPublished": false,
    "publishedAt": null
  },
  "tokens": {
    "intakeMode": "retrofit",
    "tiers": 2,
    "primitivesBuilt": true,
    "semanticBuilt": true,
    "stylesBuilt": true,
    "collections": [
      "Jake UI",
      "Color Primitives",
      "Dimensions",
      "Interaction",
      "Typography Primitives"
    ],
    "styleGroups": [
      "Text",
      "Elevation"
    ],
    "lastSync": "2026-08-09T07:30:00Z"
  },
  "sheets": {
    "built": false
  },
  "icons": {
    "library": "phosphor",
    "version": null,
    "built": true,
    "packageInstalled": false,
    "subset": []
  },
  "components": {
    "built": [
      "Input"
    ],
    "meta": {
      "Input": {
        "status": "draft",
        "updatedAt": "2026-08-09T06:05:00Z",
        "_note": "Code implemented, CI-bound, and now RENDERED and visually verified in Storybook across all five states in both modes. Still draft: promotion to stable is storybook-chromatic-builder's call and there is no Chromatic baseline. Rendering caught a real bug (see the cn/tailwind-merge fix) and disproved part of the focus-ring rationale."
      }
    },
    "instanceSwapUpgradePending": []
  },
  "repo": {
    "stage": "github",
    "packageManager": "npm",
    "monorepo": null,
    "remote": "https://github.com/liquidjakey/jakeui",
    "visibility": "private",
    "_note": "Single-package repo, not a monorepo. Docs were moved out of the bkit-style docs/02-design/ path to a flat docs/ on 9 Aug 2026 and narrowed to build-relevant content only; historical material (CRRT benchmark, the 8 Aug audit snapshot, the Figma cleanup backlog) moved to notes/. Convert to a workspace only once component packages actually exist. No GitHub Actions workflow yet — `npm run check` runs both gates locally."
  },
  "sync": {
    "platforms": [],
    "customAdapters": [],
    "lastRun": null
  },
  "storybook": {
    "initialized": true,
    "chromatic": false,
    "codeConnect": false,
    "_note": "Storybook 9 + Vite + Tailwind v4 at the repo root, not in a monorepo. storybook-chromatic-builder expects a pnpm/Turborepo workspace with packages/tokens and packages/ui; this is the single-package equivalent. Chromatic deliberately deferred: it is a paid SaaS needing CI, and a baseline of one component carries little signal. Revisit once several components exist."
  },
  "audit": {
    "ranAt": "2026-08-08T00:00:00Z",
    "codeSurface": {
      "_scope": "design-system/ only; Jake UI has no consuming application code",
      "_patternSource": "shipped defaults, untuned — see notes",
      "cssCustomProps": 223,
      "cssPrimitiveTokens": 48,
      "cssSemanticTokensLight": 48,
      "cssSemanticTokensDark": 41,
      "cssTailwindBridgeTokens": 86,
      "rawHexRgba": 30,
      "scssColorVars": 0,
      "tailwindColorClasses": 0,
      "jsColorsUsages": 0,
      "svgFills": 0,
      "codeComponents": 1,
      "figmaMapComponents": 74,
      "figmaMapWithCodePath": 1,
      "figmaMapDecomposeEntries": 23
    },
    "figmaInventory": {
      "variables": 180,
      "collections": 5,
      "bindings": 65045,
      "bindingsUnresolved": 0,
      "bindingsByCollection": {
        "Dimensions": 41244,
        "Jake UI": 12379,
        "Typography Primitives": 11272,
        "Interaction": 120,
        "Color Primitives": 30
      },
      "textStyles": 14,
      "effectStyles": 9,
      "paintStyles": 0,
      "gridStyles": 0,
      "pages": 58,
      "componentSets": 75,
      "components": 680,
      "instances": 2278,
      "modes": [
        "Light",
        "Dark"
      ],
      "_modesNote": "Light/Dark on the 'Jake UI' collection only; the four primitive collections are single-mode ('Value')"
    },
    "percentSemantic": 100,
    "_findings": {
      "rawColorBindings": "All 30 are documentation swatch frames on the '↳ Color' page, binding primitives by design. Not a defect — color is 100% semantic in product surfaces.",
      "exceptionTokens": "RESOLVED 9 Aug 2026. 24 of 26 'exception tokens' carried 4814 live bindings (line-height/18: 1359, size/13: 1345, space/1-75: 402, radius/7: 308); size/13 + line-height/18 IS Label/MD and Body/SM, the two most-used text styles. All 24 promoted to ramp members: Figma descriptions rewritten with per-token binding counts and an explicit do-not-delete. size/20 and line-height/12 verified unused across nodes, aliases, and styles, then deleted (182 -> 180 variables). Post-write verification: 65045 bindings intact, 0 unresolved, 180/180 variables still described.",
      "exportGap": "RESOLVED 9 Aug 2026. globals.css is now generated and covers 176/180 variables individually plus 14 text styles and 9 effect styles. The 4 uncovered (style/regular|medium|semibold|bold) are Figma font-style names already aliased to weight/*. Shadows reached CSS for the first time. 248 declarations, up from 181.",
      "missingGenerator": "RESOLVED 9 Aug 2026. scripts/export-tokens.mjs and scripts/figma-token-query.js written (query-in-Figma -> commit dump -> transform in Node, mirroring the figma.map.json pipeline, because the Variables REST API is Enterprise-only). tokens/annotations.json holds human prose so regeneration never destroys it. Verification: all 181 previously-present declarations resolve to identical computed values, 0 reference cycles, 0 unresolved var() references. Fixed a latent defect in the process: --font-sans: var(--font-sans) was self-referential in the old @theme block.",
      "rollbackBaseline": "Figma named version 'pre-throughline-retrofit', id 2385260588620484892, created 2026-08-08T09:54:59Z by jjyeon. Restore point for all retrofit writes."
    },
    "_percentSemanticNote": "99 = share of COLOR bindings reaching a role-named token (11015/11045; only 30 bind raw color). Across ALL bindings the semantic share is 19% (12379/65045), because dimension and type bind directly to scale primitives by design (shadcn/Tailwind convention), not by omission. Zero unresolved or loose values, so this is a rename-in-place + cleanup job, not a rewrite.",
    "docSurface": {
      "documented": 86,
      "undocumented": 0,
      "sources": {
        "figmaDescription": 86,
        "markdown": 1,
        "readme": 1,
        "codeJsdoc": 0,
        "mdx": 0
      },
      "_note": "86 = 75 component sets + 11 standalone, all carrying verified Figma descriptions; 182/182 variables described too. The gap is code-side: only 1 of 86 (input.md) has a written code doc, and 0 code components exist. The retrofit docs phase adopts the Figma descriptions rather than authoring from scratch."
    }
  },
  "tokenCrosswalk": {
    "path": null,
    "statusCounts": null,
    "validatorPassing": null
  },
  "retrofit": {
    "phase": "rebind",
    "startedAt": "2026-08-09T06:20:00Z",
    "completedAt": null,
    "journalScaffolded": true,
    "_shape": "Brownfield in Figma, greenfield in code. Figma is mature (75 sets, 180 variables, 65045 bindings); the repo has no consuming application code (codeSurface: scssColorVars 0, tailwindColorClasses 0, jsColorsUsages 0). Phases 2 (refine), 3 (rebind) and 6.5 (docs) carry real work. Phase 4 (sync) conflicts with the existing working export-tokens.mjs pipeline. Phases 5 (baseline), 6 (code) and 7 (cleanup) are largely empty or blocked because there is no consuming code to protect — see the phase map in the handoff.",
    "_outOfOrderNote": "components/input.tsx was written before entering this sequence (no skill loaded). Assessed low-harm: it binds tokens by name (card, input, ring, destructive, muted, muted-foreground, foreground), so Phase 2 value changes propagate automatically, and no Chromatic baseline existed to invalidate.",
    "_refineProgress": "COMPLETE. muted-foreground darkened 9 Aug 2026 after visual review: Light neutral/500 -> neutral/600 across 2206 bindings; Dark unchanged at neutral/400. All Light pairings now pass AA (card 7.81, muted 7.48, accent 7.16, secondary 7.10, sidebar 7.48); Dark 5.74-6.91. Binding count 65088 -> 65088 (delta 0), baseline taken in the same execution as the write this time. The comparison frame was deleted afterwards (it read 'now - neutral/500', which had become false) and the file returned to exactly 65045 bindings. Added 8 primitives (neutral/300,600,700; red/300,700; green/700; amber/700; blue/900) + primary-readable. Realigned 5 semantic tokens in place: destructive-hover (was inert), primary-active (collided with hover in Light), accent-hover (was borrowing zinc/800), success-muted-foreground and warning-muted-foreground (both resolved to the same primitive as their solid token, so the audit's 'rebind Alert text' fix would have been a no-op). Binding-survival audit PASSED: 65045 before, 65045 after, delta 0; style-level refs 42 -> 42; 0 unresolved aliases; 189/189 described. Zero renames were needed. OPEN: muted-foreground -> neutral/600 (2206 bindings) awaiting visual review; comparison frame rendered on the '↳ Color' page.",
    "_rebindProgress": "Complete. Rebound 75 text fill bindings across 71 nodes: 71 primary -> primary-readable (Light unchanged at 6.82 since both resolve to blue/700; Dark 2.03 -> 9.89 on card/popover, 2.24 -> 10.93 on background), 2 warning -> warning-muted-foreground (3.07 -> 4.84), 2 success -> success-muted-foreground (3.15 -> 4.79). Deliberately NOT rebound: destructive (62 text bindings, passes at 4.76 Light / 6.19 Dark, and destructive-foreground is white so it is not a valid substitute) and info (4.82 / 4.88, passes). Binding total 65088 vs baseline 65045; the +43 is entirely the muted-foreground comparison frame created for the deferred grey decision (verified: total minus that frame == 65045 exactly). Zero text nodes ended up with multiple bound fills, so no over-application.",
    "_rebindOpenItems": "3 destructive-as-text nodes on accent in Light = 4.37 (below 4.5). 2 destructive-as-text nodes sit on a LITERAL (unbound) background and read 2.89 in Dark — the literal fill is itself a token violation and should be bound before the contrast can be fixed. Neither is addressable by rebinding alone.",
    "_phase4Declined": "token-sync-layer NOT adopted; export-tokens.mjs kept. It already satisfies 4 of the skill's hard rules (float32 rounding at the export boundary, semantic->primitive var() references preserved, --check gate against hand-edits, committed diffable dump), and adopting Phase 4 would require converting to a monorepo for packages/tokens while NOT removing the query-in-Figma step, since that exists because the Variables REST API is Enterprise-only on this plan. Ported the opacity rule immediately (it exposed a live 120-binding bug). Deferred: rename detection (revisit before scaling component count), channel-alpha (when a consumer exists), multi-platform adapters (when a non-web target is real). Full reasoning in docs/design-system/decisions/2026-08-09-phase-4-sync-declined.md.",
    "_phase5and6Skipped": "baseline (5) blocked: no Storybook/Chromatic, and storybook-chromatic-builder requires a monorepo + packages/tokens. code (6) is N/A as a retrofit: no consuming application, scssColorVars/tailwindColorClasses/jsColorsUsages all 0, so there is nothing to swap and no consumer to keep working. Next actionable phase is 6.5 (docs).",
    "_phase5Note": "baseline (Chromatic) still not captured and deliberately so. Storybook WAS set up, but for its own reasons rather than phase 5: globals.css had never been compiled (307 declarations, no Tailwind installed) and no component had ever been rendered. Phase 5 exists to protect a code retrofit that is N/A here."
  },
  "completedSkills": [
    "figma-environment-setup",
    "design-system-audit"
  ]
}
